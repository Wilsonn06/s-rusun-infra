apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rusun-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: localhost
    http:
      paths:
      - path: /adm
        pathType: Prefix
        backend:
          service:
            name: s-rusun-adm
            port:
              number: 3001
      - path: /app
        pathType: Prefix
        backend:
          service:
            name: s-rusun-app
            port:
              number: 3002
