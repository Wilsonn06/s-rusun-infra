apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rusun-ui-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: s-rusun-ui
                port:
                  number: 80
